# JitPack 构建配置
# https://jitpack.io/docs/BUILDING/
# JitPack 使用此文件确定构建环境

jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.19-tem
  - sdk use java 17.0.19-tem

install:
  - ./gradlew :myOaid:assembleRelease :msaOaid:assembleRelease --no-daemon
